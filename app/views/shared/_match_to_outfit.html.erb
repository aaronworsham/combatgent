<% if product.outfit.present? %>
  <%= form_tag outfit_url(product.outfit), :method => :get do %>
    <%= hidden_field_tag :product_id, product.id %>
    <%= submit_tag "Match" %>
  <% end %>
<% end %>