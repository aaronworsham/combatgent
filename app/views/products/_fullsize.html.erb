<% if product.images.size > 1 %>	
 		<%= image_tag product.images.second.attachment.url(:large), :class => 'zoom-image' %>	
<% else %>
		<%= image_tag product.images.first.attachment.url(:large), :class => 'zoom-image' %>
<% end %>